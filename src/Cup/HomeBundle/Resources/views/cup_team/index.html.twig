{% extends 'CupHomeBundle::layout.html.twig' %}

{% block body -%}
    <h1>cup_team list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Logo</th>
                <th>Cup</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cup_team_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.logo }}</td>
                <td>{{ entity.cup }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cup_team_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cup_team_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cup_team_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
